apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: movieapp
  labels:
    app: backend
spec:
  type: ClusterIP  # Internal service, accessible only within cluster
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: backend

