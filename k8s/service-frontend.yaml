apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: movieapp
  labels:
    app: frontend
spec:
  type: LoadBalancer  # Use NodePort for local clusters, LoadBalancer for cloud
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: frontend

